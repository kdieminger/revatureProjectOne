// import { DocumentClient } from 'aws-sdk/clients/dynamodb';
// import { timeStamp } from 'console';
// import dynamo from '../src/dynamo/dynamo';
// import carService from '../src/car/car.service';

// let cars = [{brand: 'Honda', etc.}]
// let obj = {promise: jest.fn().mockResolvedValue(items)};
// dynamo.scan = jest.fn().mockReturnValue(obj);

// test('that a promise is returned with the cars in it', async () => {
//     await carService.getCars().then((cars) => {
//         returnValues = cars;
//     });
//     expect(returnValues).toBe(cars);
// })